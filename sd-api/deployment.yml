apiVersion: apps/v1
kind: Deployment
metadata:
  name: msa-sample-sd-api
  labels:
    app: msa-sample-sd-api
spec:
  replicas: 1
  selector:
    matchLabels:
      app: msa-sample-sd-api
      tier: backend
  template:
    metadata:
      labels:
        app: msa-sample-sd-api
        tier: backend
    spec:
      containers:
        - name: msa-sample-sd-api
          image: harbor.k-paas.org/openlab/msa-sample-sd-api:latest
          imagePullPolicy: IfNotPresent
          resources:
            requests:
              memory: "256Mi"
              cpu: "50m"
            limits:
              memory: "1Gi"
              cpu: "500m"
          ports:
            - containerPort: 8084
          envFrom:
            - configMapRef:
                name: kpaas-msa-configmap
            - secretRef:
                name: mariadb-secret
